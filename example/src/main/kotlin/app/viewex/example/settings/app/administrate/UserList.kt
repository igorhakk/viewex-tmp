package app.viewex.example.settings.app.administrate

import app.viewex.example.settings.app.SettingsApp
import app.viewex.composer.LayoutParams
import app.viewex.layout.tmp.View

class UserList(app: SettingsApp) : Page(app) {

    override fun getLayout(params: LayoutParams): View {
        TODO("Not yet implemented")
    }
}
